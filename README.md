```bash
export PRIME_API_KEY=your_key
```

```bash
pip install openai
```

```bash
python game.py           # run a game
python game.py -s        # spoiler mode (see who impostors are)
python stats.py          # view stats for past games
```
